
import os
from setuptools import setup, find_packages

setup(
    name="lang_sam",
    py_modules=["lang_sam", "utils"],
    version="1.0.0",
    description="",
    author="",
    packages=find_packages(),
)
